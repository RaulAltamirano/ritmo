/**
 * Reexporta tokens de `@ritmo/ui` (vía alias de Vite) con resolución estable para TypeScript.
 * Ver `packages/ui/src/tokens/colors.ts` — `interactiveOption` / `getInteractiveOptionClasses`.
 */
export { getInteractiveOptionClasses } from '@ritmo/ui/tokens/colors'
