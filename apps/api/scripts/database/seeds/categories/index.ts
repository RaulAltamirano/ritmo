// Placeholder for category seeds
// This can be expanded when categories are added to the schema

export async function seedCategories(prisma: any) {
  console.log('📂 Seeding categories...')
  // TODO: Implement when categories are added to the schema
  console.log('✅ Categories seeding completed (no categories to seed)')
  return []
}
